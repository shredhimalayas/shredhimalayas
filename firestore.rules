rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read & write access for site content and admin authentication
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
